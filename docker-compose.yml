services:
  mock-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mock-backend
    ports:
      - "4000:4000"
    deploy:
      resources:
        limits:
          cpus: '4'
    restart: unless-stopped